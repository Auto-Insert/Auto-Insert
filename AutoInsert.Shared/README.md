# AutoInsert.Shared

This project is responsible for organizing components that are shared between all of the projects in the solution.

---

### Structure

- `Models/` — plain data classes shared across projects.

- `Interfaces/` — class abstractions.
