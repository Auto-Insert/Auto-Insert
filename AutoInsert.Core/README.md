# AutoInsert.Core

This project is responsible for the business logic.

---

### Structure

- `Controllers/` — interfaces or entry points that connect services to callers.

- `Services/` — business logic implementations.

- `Utilities/` - utility methods/classes that are used across several components as helpers.